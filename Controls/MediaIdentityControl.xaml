<UserControl
    x:Class="ModernIPTVPlayer.Controls.MediaIdentityControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ctl="using:ModernIPTVPlayer.Controls"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <StackPanel x:Name="RootPanel" Spacing="16" HorizontalAlignment="Left" Margin="0">
        <!-- IDENTITY CONTAINER (SuperTitle / Category Branding) -->
        <StackPanel x:Name="IdentityContainer" Spacing="0" Margin="0" Visibility="Collapsed">
             <StackPanel x:Name="IdentityStack" Spacing="0">
                <TextBlock x:Name="SuperTitleText" Text="" Foreground="#88FFFFFF" FontSize="14" FontWeight="SemiBold" CharacterSpacing="100"/>
             </StackPanel>
        </StackPanel>

        <!-- TITLE / LOGO SLOT -->
        <Grid x:Name="TitleGroup" VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="0,0,0,2">
            <StackPanel x:Name="TitlePanel" Opacity="1" Spacing="12">
              <Border x:Name="ContentLogoHost" Height="120" Width="420" MaxHeight="120" MaxWidth="420" 
                      HorizontalAlignment="Left" Visibility="Collapsed" Margin="0,0,0,0" VerticalAlignment="Bottom">
                  <Image x:Name="ContentLogoImage" Stretch="Uniform" HorizontalAlignment="Left" VerticalAlignment="Bottom"
                         ImageOpened="ContentLogoImage_ImageOpened" ImageFailed="ContentLogoImage_ImageFailed"/>
              </Border>

              <TextBlock x:Name="TitleText" Text="Loading..." FontSize="48" FontWeight="Black" Foreground="White" 
                         TextWrapping="Wrap" MaxLines="2" TextTrimming="CharacterEllipsis"
                         VerticalAlignment="Stretch"/>
            </StackPanel>
            
            <ctl:ShimmerControl x:Name="TitleShimmer" Width="400" Height="56" CornerRadius="8" Visibility="Collapsed"
                                HorizontalAlignment="Left" VerticalAlignment="Bottom"/>
        </Grid>
    </StackPanel>
</UserControl>
