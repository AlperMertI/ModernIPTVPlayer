<UserControl
    x:Class="ModernIPTVPlayer.Controls.DownloadStatusButton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ModernIPTVPlayer.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.Resources>
        <SolidColorBrush x:Key="ActiveProgressBrush" Color="DeepSkyBlue"/>
        <SolidColorBrush x:Key="PausedProgressBrush" Color="#FFFFA500"/> <!-- Orange -->
    </UserControl.Resources>

    <Grid x:Name="ContainerGrid">
        <Button x:Name="RootButton"
                Width="44" Height="44"
                Padding="0"
                Background="Transparent"
                BorderThickness="0"
                Click="RootButton_Click"
                ToolTipService.ToolTip="İndirmeler"
                CornerRadius="22">
            <Grid>
                <!-- Background Ring (Track) - Made larger and thinner -->
                <Ellipse x:Name="ProgressTrack" Width="38" Height="38" Stroke="#15FFFFFF" StrokeThickness="1.5" Visibility="Collapsed"/>
                
                <!-- Progress Ring - Positioned further out from the icon -->
                <ProgressRing x:Name="GlobalProgressRing" 
                              Width="38" Height="38" 
                              IsIndeterminate="False" 
                              Value="0" 
                              Foreground="{StaticResource ActiveProgressBrush}"
                              Visibility="Collapsed"
                              Background="Transparent"/>

                <!-- Icon - Slightly smaller for elegance -->
                <FontIcon Glyph="&#xE896;" FontSize="16" Foreground="White" Opacity="0.9"/>
            </Grid>
        </Button>
    </Grid>
</UserControl>
